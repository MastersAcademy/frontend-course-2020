import { CurrencyCodeCarPipe } from './currency-code-car.pipe';

describe('CurrencyCodeCarPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyCodeCarPipe();
    expect(pipe).toBeTruthy();
  });
});
